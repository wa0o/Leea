运行该软件时需注意：

(1) 输入想训练的股票数据的代号后，需点击“确认”键，否则采用默认的“603758”股票数据集。股票数据的代号可参考“股票代号”文档。若输入了不存在或错误的股票代号，训练无法进行。

(2) 若不使用默认参数进行训练，则需要在参数输入栏输入相应合理的参数，后点击“确认”键，随后即可开始训练，否则依然使用默认参数进行训练。

(3) 由于部分算法的运行时间较长，在点击“开始训练”键后可能需要等待较长时间。

(4) 若想返回程序上一级，可点击页面右上角的“X”号。

(5) 请勿移动、修改或删除Stock_Prediction软件中的文件。